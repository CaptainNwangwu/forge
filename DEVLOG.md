# Phase 1 — CLI Skeleton (2026-03-26)

- Implemented CLI parsing with clap (derive API)
- Feature flags: --db, --auth, --middleware, --surface
- ScaffoldConfig struct with strongly-typed enum fields
- dotnet SDK version detection via std::process::Command
